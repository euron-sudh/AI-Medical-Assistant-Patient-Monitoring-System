"use client";
interface AISummaryDisplayProps { summary: string | null; analysis: Record<string, unknown> | null; status: string; }
export default function AISummaryDisplay({ summary, analysis, status }: AISummaryDisplayProps) {
  if (!summary && !analysis) { if (status === "pending" || status === "processing") return (<div className="rounded-md border border-blue-200 bg-blue-50 p-4"><div className="flex items-center gap-3"><div className="h-5 w-5 animate-spin rounded-full border-2 border-blue-500 border-t-transparent" /><p className="text-sm font-medium text-blue-700">AI Analysis in Progress</p></div></div>); return <p className="text-sm text-muted-foreground italic">No AI analysis available.</p>; }
  const findings = analysis?.findings ?? analysis?.key_findings;
  const recs = analysis?.recommendations;
  return (<div className="space-y-4">{summary && (<div className="rounded-md border border-primary/20 bg-primary/5 p-4"><h4 className="flex items-center gap-2 text-sm font-semibold text-foreground"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-primary/10 text-xs">AI</span>AI Summary</h4><p className="mt-2 text-sm text-muted-foreground whitespace-pre-wrap">{summary}</p></div>)}{findings && Array.isArray(findings) && (<div><h4 className="text-sm font-medium text-foreground mb-2">Key Findings</h4><ul className="space-y-1.5">{(findings as string[]).map((f, i) => (<li key={i} className="flex items-start gap-2 text-sm text-muted-foreground"><span className="mt-1.5 h-1.5 w-1.5 flex-shrink-0 rounded-full bg-primary" />{f}</li>))}</ul></div>)}{recs && Array.isArray(recs) && (<div><h4 className="text-sm font-medium text-foreground mb-2">Recommendations</h4><ul className="space-y-1.5">{(recs as string[]).map((r, i) => (<li key={i} className="flex items-start gap-2 text-sm text-muted-foreground"><span className="mt-0.5 text-green-500">&#10003;</span>{r}</li>))}</ul></div>)}{analysis && !findings && !recs && (<div><h4 className="text-sm font-medium text-foreground mb-2">Detailed Analysis</h4><div className="rounded-md bg-muted/50 p-3"><pre className="text-xs text-muted-foreground whitespace-pre-wrap">{JSON.stringify(analysis, null, 2)}</pre></div></div>)}</div>);
}
